/* =========================================================================
   נתוני בתי הספר למערכת הבוגרים — קובץ אחד שמשרת גם את mazaltov.html
   (העתקת ההודעה) וגם את pgisha.html (קביעת הפגישה).

   מקור הנתונים: GET /api/admin/alumni/summary  ב-hopa.org.il
   כדי לעדכן — מעדכנים כאן בלבד, שני הדפים מתעדכנים יחד.

   slug   — מזהה קבוע לקישור האישי (pgisha.html?s=<slug>). לא לשנות אחרי שליחה!
   name   — השם המדויק כפי שהוא רשום במערכת
   alumni — כמה בוגרים במערכת
   years  — המחזורים במערכת, מהוותיק לחדש. מערך ריק = אין פילוח, ההודעה
            תיכתב בלי טווח שנים במקום להמציא אחד.
   gaps   — מה חסר ברשימות. כל פריט נכנס להודעה כשורה בבקשת ההשלמה.
            מערך ריק = אין מה לבקש, ופסקת ההשלמה לא נכתבת בכלל.
   ========================================================================= */

const DATA_DATE = '9.9.2026';

const SCHOOLS = [
  { slug:'bh',  name:'תיכון אורט בית הערבה ירושלים', alumni:438,
    years:['תשע"ט','תש"פ','תשפ"א','תשפ"ב','תשפ"ג','תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'ako', name:'אורט מרום עכו', alumni:354,
    years:['תשע"ח','תשע"ט','תש"פ','תשפ"א','תשפ"ב','תשפ"ג','תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'ask', name:'אורט אדיבי אשקלון', alumni:296,
    years:['תשפ"ב','תשפ"ג','תשפ"ד','תשפ"ה'], gaps:[] },

  { slug:'krm', name:'עתיד מגשימים כרמיאל', alumni:237,
    years:['תשפ"א','תשפ"ב','תשפ"ג','תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'mtz', name:'עתיד קריית מוצקין', alumni:141,
    years:['תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'ta',  name:'תיכון חברתי תל אביב דרור', alumni:121,
    years:['תשפ"ג','תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'hos', name:'חושן', alumni:109,
    years:['תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'siah', name:'תיכון שיח', alumni:80,
    years:['תשפ"ג','תשפ"ד','תשפ"ה','תשפ"ו'], gaps:[] },

  { slug:'bsh', name:'אורט מגשימים בית שאן', alumni:77,
    years:[], gaps:[] },

  { slug:'skh', name:'סחנין אלאמאני', alumni:75,
    years:['תשפ"ו'], gaps:[] },

  { slug:'rd',  name:'עמל רמת דוד', alumni:70,
    years:[], gaps:[] },

  { slug:'tzb', name:'אורט צור ברק', alumni:50,
    years:[], gaps:[] },

  { slug:'gut', name:'תיכון אהרון גוטווירט', alumni:36,
    years:[], gaps:[] },

  { slug:'nm',  name:'דרור נווה מדבר', alumni:20,
    years:['תשפ"ו'], gaps:[] },

  { slug:'bam', name:'עתיד אבדאע ביר אלמכסור', alumni:19,
    years:[], gaps:[] }
];

/* ===== ניסוח משותף לשני הדפים ===== */

/* "משנת תשפ״ד ועד שנת תשפ״ו" · "מחזור תשפ״ו" · ריק אם אין פילוח */
function yearsPhrase(s){
  const y = (s && s.years) || [];
  if(!y.length) return '';
  if(y.length === 1) return 'מחזור ' + y[0];
  return 'משנת ' + y[0] + ' ועד שנת ' + y[y.length-1];
}

function schoolBySlug(slug){
  return SCHOOLS.find(s => s.slug === slug) || null;
}
